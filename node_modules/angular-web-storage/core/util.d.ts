/**
 * 过期时间单位
 * s：秒
 * m：分
 * h：时
 * d：天
 * w：周
 * y：年
 * t：自定义（毫秒ms）
 */
export declare type ExpiredUnit = 's' | 'm' | 'h' | 'd' | 'w' | 'y' | 't';
export declare class StorageUtil {
    static get(storage: Storage, key: string): any;
    static set(storage: Storage, key: string, value: any, expiredAt?: number, expiredUnit?: ExpiredUnit): void;
    static remove(storage: Storage, key: string): void;
    private static getExpired(val, unit);
    private static stringify(value);
    private static parse(text);
}
