import { ExpiredUnit } from "./util";
/**
 * localStorage 装饰器
 *
 * @export
 * @param {string} [key] 指定一个新key
 * @param {number} [expiredAt=0] 过期时间值，0表示永久有效。
 * @param {ExpiredUnit} [expiredUnit='t'] 过期时间单位（默认：自定义[单位：毫秒]）
 * @returns
 */
export declare function LocalStorage(key?: string, expiredAt?: number, expiredUnit?: ExpiredUnit): (target: Object, propertyName: string) => void;
/**
 * sessionStorage 装饰器
 *
 * @export
 * @param {string} [key] 指定一个新key
 * @returns
 */
export declare function SessionStorage(key?: string): (target: Object, propertyName: string) => void;
